class Product {
  String id;
  String title;
  double value;
  bool available;
  String photoUrl;

  Product({
    this.id = '',
    this.title = '',
    this.value = 0.0,
    this.available = true,
    this.photoUrl,
  });
}
