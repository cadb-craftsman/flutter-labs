import 'package:flutter/material.dart';

class AvantarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Avantar',
      home: Scaffold(
        appBar: AppBar(
          title: Text('Avatar'),
        ),
        body: Center(),
      ),
    );
  }
}
